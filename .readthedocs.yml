version: 2

build:
  apt_packages:
    - cmake
    - libcgal-dev
  os: ubuntu-22.04
  tools:
    python: "3"
  jobs:
    install:
      # See https://docs.readthedocs.com/platform/stable/build-customization.html#install-dependencies-from-dependency-groups
      - pip install --upgrade pip
      - pip install --group 'doc' .

sphinx:
  configuration: docs/conf.py
